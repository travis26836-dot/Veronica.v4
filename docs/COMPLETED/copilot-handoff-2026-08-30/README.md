# Archived Codex-to-Copilot handoff

The session-start hook moves the finished handoff documents here at the first VS Code Copilot agent-session prompt. Its `archive-manifest.json` records the completed move.

The evidence record remains in `runs/2026-08-30-copilot-handoff/`, as required by the project contract.
